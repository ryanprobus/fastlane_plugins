describe Fastlane::Actions::CheckGoodVersionAction do
  describe '#run' do
    it 'prints a message' do
      expect(Fastlane::UI).to receive(:message).with("The check_good_version plugin is working!")

      Fastlane::Actions::CheckGoodVersionAction.run(nil)
    end
  end
end
