describe Fastlane::Actions::GoodifyInfoPlistAction do
  describe '#run' do
    it 'prints a message' do
      expect(Fastlane::UI).to receive(:message).with("The goodify_info_plist plugin is working!")

      Fastlane::Actions::GoodifyInfoPlistAction.run(nil)
    end
  end
end
